// const network = require("../Network/app");
// const BlockChain = require("../Blockchain/BlockChain");
// const { appConfig , webServerConfig } = require("../config");
// const blockChain = new BlockChain();

// // run network

// webServerConfig.port = Math.floor(Math.random() * (20000 + 40000 - 1) + 1);

// // network(blockChain,appConfig.rootSocketIP,appConfig.rootSocketPORT,webServerConfig.port);

// // process.stdout.write("welcome to dpx Coin !\n");
// // process.stdout.write("> ");
// // process.stdin.on("data",(data)=>{
// //     data = data.toString();
// //     process.stdout.write(`${data}`);
// //     process.stdout.write("> ");
// // });